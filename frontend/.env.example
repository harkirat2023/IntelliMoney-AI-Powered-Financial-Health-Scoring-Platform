# Backend API URL (set to production Render URL for deployment)
API_BASE_URL=http://localhost:8080/api/v1

# Clerk publishable key (pk_test_...) — required for Clerk-based sign-in
VITE_CLERK_PUBLISHABLE_KEY=